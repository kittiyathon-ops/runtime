# Runtime Rules

Preserve explicit runtime state transitions, fail-closed behavior, governance halt behavior, safe-mode behavior, idempotency, auditability, and operator visibility.

Do not rewrite runtime orchestration unless explicitly approved.

Prefer minimal, atomic patches.
